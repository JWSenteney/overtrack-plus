{
  "name": "overtrack-plus",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:JWSenteney/overtrack-plus.git",
  "author": "Jacob Senteney <jwsenteney@gmail.com>",
  "license": "MIT",
  "scripts": {
    "install-project": "yarn install && yarn --cwd desktop install && yarn --cwd web install",
    "electron-dev": "export BROWSER=none && concurrently \"yarn --cwd web start\" \"yarn --cwd desktop wait-react\"",
    "web-dev": "yarn --cwd web start"
  },
  "devDependencies": {
    "concurrently": "^5.0.0",
    "prettier": "^1.19.1"
  }
}
